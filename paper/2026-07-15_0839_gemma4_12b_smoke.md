# Gemma 4 12B TransformerLens Bridge smoke result

- **Produced:** 2026-07-15 08:39 Europe/Berlin
- **Model:** `google/gemma-4-12B-it`
- **Scope:** TransformerLens 3 Bridge compatibility, residual-stream hook access, native-chat decision scoring, steering intervention, and single-GPU memory feasibility
- **Status:** Complete; smoke passed. The independent 31B retry remains queued and is not evaluated here.

## Artifacts

- **Scripts:** `src/smoke_test_gemma4.py`, `src/utils/model_loader.py`, `jobs/sge/smoke_gemma4.sh`, `jobs/sge/submit_gemma4.sh`
- **Outputs:** `results/logs/smoke_gemma4_12b.json`

## Summary

Gemma 4 12B passed every smoke-test gate on one NVIDIA L40 GPU. The raw-weight TransformerLens Bridge exposed the expected residual-stream hook, matched the Hugging Face model's logits exactly for the checked forward pass, and supported an additive residual-stream intervention without producing non-finite scores. This establishes technical feasibility for the existing experiment family; it is not a scientific validation of warmth, competence, or hiring effects.

## Results

| Check | Result |
|---|---:|
| Grid Engine job | `1141615` |
| Scheduler status | `exit_status=0`, `failed=0` |
| Wall time | 152 s |
| GPU | NVIDIA L40 |
| Layers | 48 |
| Residual width | 3,840 |
| Probe layer | 31 (`blocks.31.hook_resid_post`) |
| Activation shape | `[1, 9, 3840]` |
| Bridge/HF maximum logit difference | 0.0 |
| Baseline Yes/No margin | 17.556884765625 |
| Steered Yes/No margin | 17.574462890625 |
| Peak allocated VRAM | 22.5006 GiB |
| Prompt format | native chat |

The selected layer follows the configured `probing.probe_layer_frac = 0.66` convention: `floor(48 x 0.66) = 31`. The nonzero margin change confirms that the hook intervention reached the forward pass. Its sign and magnitude have no substantive interpretation because the smoke uses a synthetic test direction rather than a learned warmth or competence vector.

## Runtime

- TransformerLens 3.5.1
- Transformers 5.13.0
- PyTorch 2.13.0+cu130
- `torch.bfloat16`
- Seed 20260527

## Operational follow-up

The corrected Gemma 4 31B retry is Grid Engine job `1141614`, restricted to `gpu@scc214` with one GPU and `h_vmem=96G`. At the time of this report it remained in `qw`, so there is no 31B pass/fail result yet. The already validated 26B-A4B MoE smoke was intentionally not resubmitted, and no full experimental pipeline was launched.
