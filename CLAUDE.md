# Repository Context for Claude

All project context, rules, and conventions live in [`AGENTS.md`](./AGENTS.md). Read that file at the start of every session before doing anything else.

This file exists only to ensure Claude Code picks up the same context as other AI agents. The single source of truth is `AGENTS.md`.
