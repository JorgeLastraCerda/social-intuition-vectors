# Story generation (CPU / laptop — no GPU needed for this step)
anthropic>=0.40.0
tqdm>=4.66.0

# Model probing and steering (GPU — run on SCCKN)
torch
transformers
accelerate

# TransformerLens path (Gemma 3, Qwen — conda env: wc-tl)
transformer-lens
sae-lens           # GemmaScope 2 SAE decomposition

# nnsight path (Gemma 4 — conda env: wc-nn)
nnsight
nnterp

# Data and numerics
numpy
pandas
scikit-learn
scipy

# Plotting
matplotlib
seaborn

# Config and utilities
pyyaml
jupyter
pymupdf
datasets  # login-node only: scripts/build_neutral_corpus.py
